// {
//   // IntelliSense を使用して利用可能な属性を学べます。
//   // 既存の属性の説明をホバーして表示します。
//   // 詳細情報は次を確認してください: https://go.microsoft.com/fwlink/?linkid=830387
//   "version": "0.2.0",
//   "configurations": [


//     {
//       "type": "pwa-node",
//       "request": "launch",
//       "name": "Launch Program",
//       "skipFiles": [
//         "<node_internals>/**"
//       ],
//       "program": "${workspaceFolder}/app/javascript/scripts/order.js",
//       "preLaunchTask": "npm: build",
//       "outFiles": ["${workspaceFolder}/dist/*.js"]
//     }
//   ]
// }

{
  "version": "0.1.0",
  "configurations": [

    // ローカルにあるファイルをデバッグ
    {
      // デバッグ設定の名称
      "name": "Launch local file",
      // デバッグ種別を指定（chrome/node/monoなど）
      "type": "chrome",
      // lanunchまたはattachを指定
      "request": "launch",
      // 読み込むHTMLファイルを指定（プロジェクトからのパスを指定）
      "file": "${workspaceRoot}/index.html.erb"
    },

    // Webサーバー上にあるファイルをデバッグ
    {
      // デバッグ設定の名称
      "name": "Launch remote website with sourcemaps",
      // デバッグ種別を指定（chrome/node/monoなど）
      "type": "chrome",
      // lanunchまたはattachを指定
      "request": "launch",
      // Webサーバー上にあるデバッグ対象のURLを指定
      "url": "http://localhost:3000/#",
      // マッピングするローカルファイルが配置されているディレクトリを指定
      // マッピングするローカルファイルが配置されているディレクトリを指定
      "webRoot": "${workspaceRoot}/",
      // リモートデバッギングポートのポート番号を指定（デフォルト値の9222を指定）
      "port": 5500,
      // ソースマップの有効・無効の指定（true/false）
      "sourceMaps": true,
      // コンソールにログを出力するかの指定（true/false）
      "trace": true
    }
  ]
}